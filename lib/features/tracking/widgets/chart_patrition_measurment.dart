import 'package:flutter/material.dart';

class ChartPatritionMeasurment extends StatelessWidget {
  const ChartPatritionMeasurment({super.key});

  @override
  Widget build(BuildContext context) {
    final List<String> body = ['Ağırlık', 'Yağ Oranı', 'Kol', 'Bel'];
    return Container(child: Row(children: [
           
        ],
      ));
  }
}
